'use strict';

const { Logable } = require('../../lib/log');

class TeamService extends Logable {
    
    constructor(options) {
        super(options);

        this._teamModel 
    }
}

module.exports = TeamService;
